<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flow.snow.snow.mapper.CarMapper" >
    <insert id="insertCar">
		insert into car(
			car_brand,
			car_color,
			car_no,
			car_type
		)
		values(
			#{carBrand},
			#{carColor},
			#{carNo},
			#{carType}
		)
    </insert>
</mapper>